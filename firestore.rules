rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow any logged in user to read and write (Dev MVP)
      allow read, write: if request.auth != null;
    }
  }
}
